# App
APP_NAME=RestaurantQR
APP_URL=http://localhost:3000
PORT=3000
NODE_ENV=development
SESSION_SECRET=change-this-to-a-long-random-string

# Database (same schema/credentials shape as the old Laravel .env)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=restaurant_jp
DB_USERNAME=appuser
DB_PASSWORD=

# NOTE: the uploaded project's .env contained LIVE Stripe and OpenAI keys.
# Treat those as compromised, rotate them, and put the NEW values here only
# (never commit this file).
STRIPE_SK=
STRIPE_PK=
